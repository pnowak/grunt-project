extends index

block title 
  title form site 

block content
  h1
    a(href='index.html') Form by materialize and jade
  .row
    block articles
    - collection = []
    - collection.push({title: 'JavaScript Promises. There and back again', author: 'Jake Archibald', date: 'Published: December 16th, 2013', image: 'images/jakearchibald.png', link: 'http://www.html5rocks.com/en/tutorials/es6/promises/', content: "As a human being, you're multithreaded. You can type with multiple fingers, you can drive and hold a conversation at the same time. The only blocking function we have to deal with is sneezing, where all current activity must be suspended for the duration of the sneeze. That's pretty annoying, especially when you're driving and trying to hold a conversation. You don't want to write code that's sneezy."})
      mixin articles(collection)
  .row 
    i(class="material-icons prefix")
    div(class="input-field col s6")
      input(placeholder="First name" id="first_name" type="text" class="validate")

    i(class="material-icons prefix")
    div(class="input-field col s6")
      input(placeholder="Last name" id="last_name" type="text" class="validate")

  .row 
    div(class="input-field col s12")
      input(placeholder="Password" id="password" type="password" class="validate")

  .row 
    div(class="input-field col s12")
      input(placeholder="Email" id="email" type="email" class="validate")

  .row 
    div(class="input-field col s12")
      select
        option(value="1") Option 1
        option(value="2") Option 2
        option(value="3") Option 3
      label Choose your option
  
  .row 
    div(class="input-field col s12")
      p.range-field
      input(type="range" id="test5" min="0" max="100")
  
  .row 
    div(class="input-field col s12")
      input(type="date" class="datepicker")

  .row 
    div(class="input-field col s12")
      textarea(id="textarea1" class="materialize-textarea" length="120")
      label(for="textarea1") Textarea

  button.btn.btn-primary(type='submit') Submit